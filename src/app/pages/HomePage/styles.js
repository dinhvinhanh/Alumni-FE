import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  wrapperIntro: {
    width: '500px',
    height: '300px',
    bottom: '-80px',
    left: '150px',
  }
}));
export default useStyles;