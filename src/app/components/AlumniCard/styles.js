import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  imageCard: {
    // width: '150px',
    margin: '0 auto',
    maxHeight: '200px',
    height: '200px'
  }
}));
export default useStyles;